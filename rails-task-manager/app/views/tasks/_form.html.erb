<%= form_for @task do |f| %>
  <%= f.label :name, 'Enter Task Name:' %>
  <%= f.text_field :name %>

  <%= f.label :done, 'Done:' %>
  <%= f.select :done, options_for_select([false, true], @task.done) %>

  <%= f.submit %>
<% end %>
